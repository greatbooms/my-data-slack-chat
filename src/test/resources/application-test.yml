spring:
  config:
    activate:
      on-profile: test
  jpa:
    hibernate:
      ddl-auto: validate
my-data:
  slack:
    signing-secret: test-signing-secret
  embedding:
    model: deterministic-1536
    dimensions: 1536
